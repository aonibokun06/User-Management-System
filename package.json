{
  "name": "springtest",
  "version": "1.0.0",
  "description": "A full-stack CRUD application with Spring Boot backend and Next.js frontend.",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "mvn clean package",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["spring-boot", "nextjs", "fullstack", "crud", "typescript"],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
